<?php
/**
 * Implementation of hook_menu
 */

function node2node_menu() {
  $items['admin/settings/node2node'] = array(
      'title' => t(node2node-Configuration),
      'description' => t('Configure node-types on which node2node should run'),
      'page callback' => 'drupal_get_form',
      'page arguments' => array('node2node_admin_settings'),
      'access_arguments' => array('administer site configuration')
  );#TODO: Write out $items['admin/build/node2node'] in node2node_menu()
}
/**
 * implementation of hook_node_form_alter
 */

function node2node_node_form_alter(&$form,&$form_state,$form_id){
  $form['node2node_button'] = array(
    '#weight' => 0,
    '#type' => 'button',
    '#button_type' => 'node2node',
    '#title' => t('Insert your nodelinks to body'),
    '#ahah' => array(
      'path' => '',  //TODO: implement hook_menu to build a url for aha-path  
      'progress' => array(
        'type' => 'throbber'
      ),
      'wrapper' => 'node2node-selection',
      'method' => 'replace'
    ),
  );
}
#TODO: build a configuration-page
#TODO: build a url, containing mycontent-view
#TODO: override the method of the AHAH-Event-Listener
